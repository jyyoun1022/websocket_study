spring:
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: true
  logging:
    level:
      org.apache.http: DEBUG
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/lab?useSSL=false&allowPublicKeyRetrieval=true
    username: root
    password: Yjy^^46232891
  profiles:
    include: oauth
